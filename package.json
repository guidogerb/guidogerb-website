{
  "name": "guidogerb-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "@guidogerb/components/api",
    "@guidogerb/components/auth",
    "@guidogerb/components/menu",
    "@guidogerb/components/pages/*",
    "@guidogerb/components/router/*",
    "@guidogerb/css",
    "@guidogerb/footer",
    "@guidogerb/header",
    "website"
  ],
  "scripts": {
    "clean": "rimraf --glob \"@guidogerb/**/dist\" \"website/dist\"",
    "build:modules": "npm run --workspaces --if-present build",
    "build": "npm install && npm run clean && npm run build:modules && npm run build -w website",
    "dev:local": "npm install && npm run clean && npm run build:modules && npm run dev -w website",
    "ci:deploy:main": "npm ci && npm run build:modules && npm run build:staging -w website",
    "ci:deploy:prod": "npm ci && npm run build:modules && npm run lint -w website && npm run build:prod-bundle -w website"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}

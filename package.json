{
  "name": "guidogerb-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "@guidogerb/components/api-client",
    "@guidogerb/components/auth",
    "@guidogerb/components/menu",
    "@guidogerb/components/pages/*",
    "@guidogerb/components/router/*",
    "@guidogerb/css",
    "@guidogerb/footer",
    "@guidogerb/header",
    "websites/garygerber.com",
    "websites/ggp-store.com",
    "websites/picklecheeze.com",
    "websites/this-is-my-story.org",
    "websites/stream4cloud.com"
  ],
  "scripts": {
    "clean": "rimraf --glob \"@guidogerb/**/dist\" \"websites/**/dist\"",
    "build:modules": "npm run --workspaces --if-present build",
    "build": "npm install && npm run clean && npm run build:modules && npm run build -w websites/stream4cloud.com",
    "dev:local": "npm install && npm run clean && npm run build:modules && npm run dev -w websites/stream4cloud.com",
    "ci:deploy:main": "npm ci && npm run build:modules && npm run build:staging -w websites/stream4cloud.com",
    "ci:deploy:prod": "npm ci && npm run build:modules && npm run lint -w websites/stream4cloud.com && npm run build:prod-bundle -w website"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
